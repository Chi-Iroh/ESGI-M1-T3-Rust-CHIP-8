[package]
name = "esgi-rust-chip8"
version = "0.1.0"
edition = "2024"

[dependencies]
# Par défaut, macroquad ne fournit pas le module audio
# Il faut le demander explicitement comme ci-dessous
macroquad = { version = "0.4.15", features = ["audio"] }
